import { environment } from 'src/environments/environment';

export const LIST_COMMITS_BACK = `${environment.API_URL}/commit/list-commit-back`;
export const LIST_COMMITS_FRONT = `${environment.API_URL}/commit/list-commit-front`;
